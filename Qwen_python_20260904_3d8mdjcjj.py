import os

base_dir = r"C:\Users\20233280187\Desktop\local-pdf-tools"
html_path = os.path.join(base_dir, "app", "static", "index.html")

html_content = '''<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Local PDF Tools</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml;base64,">
    <style>
        :root { --bg-primary: #0f1a17; --bg-secondary: #152522; --bg-gradient: linear-gradient(135deg, #0f1a17 0%, #152522 100%); --text-primary: #f2f7f4; --text-secondary: #8fd9bd; --text-muted: #6b8a80; --text-footer: #4c6459; --accent: #2fbf87; --accent-dark: #14856a; --border-color: rgba(47, 191, 135, 0.2); --border-hover: rgba(47, 191, 135, 0.4); --card-bg: rgba(47, 191, 135, 0.05); --card-border: rgba(47, 191, 135, 0.2); --btn-bg: rgba(47, 191, 135, 0.1); --btn-border: rgba(47, 191, 135, 0.3); --btn-hover: rgba(47, 191, 135, 0.2); --btn-active: rgba(47, 191, 135, 0.3); --workspace-bg: rgba(15, 26, 23, 0.6); --upload-bg: rgba(47, 191, 135, 0.05); --file-bg: rgba(47, 191, 135, 0.1); --status-success-bg: rgba(47, 191, 135, 0.2); --status-error-bg: rgba(226, 87, 76, 0.2); --status-processing-bg: rgba(143, 217, 189, 0.2); --status-warning-bg: rgba(243, 156, 18, 0.2); --shadow: rgba(0, 0, 0, 0.3); }
        [data-theme="light"] { --bg-primary: #f8f9fa; --bg-secondary: #ffffff; --bg-gradient: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%); --text-primary: #2c3e50; --text-secondary: #14856a; --text-muted: #5a6c5d; --text-footer: #7f8c8d; --accent: #2fbf87; --accent-dark: #14856a; --border-color: rgba(47, 191, 135, 0.3); --border-hover: rgba(47, 191, 135, 0.6); --card-bg: rgba(47, 191, 135, 0.08); --card-border: rgba(47, 191, 135, 0.3); --btn-bg: rgba(47, 191, 135, 0.15); --btn-border: rgba(47, 191, 135, 0.4); --btn-hover: rgba(47, 191, 135, 0.25); --btn-active: rgba(47, 191, 135, 0.35); --workspace-bg: rgba(255, 255, 255, 0.8); --upload-bg: rgba(47, 191, 135, 0.08); --file-bg: rgba(47, 191, 135, 0.15); --status-success-bg: rgba(47, 191, 135, 0.15); --status-error-bg: rgba(226, 87, 76, 0.15); --status-processing-bg: rgba(143, 217, 189, 0.15); --status-warning-bg: rgba(243, 156, 18, 0.15); --shadow: rgba(0, 0, 0, 0.1); }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: var(--bg-gradient); color: var(--text-primary); min-height: 100vh; padding: 20px; transition: background 0.3s ease, color 0.3s ease; }
        .container { max-width: 1200px; margin: 0 auto; }
        .header { margin-bottom: 40px; text-align: center; }
        .header svg { max-width: 100%; height: auto; filter: drop-shadow(0 4px 6px var(--shadow)); }
        .theme-toggle { position: fixed; top: 20px; right: 20px; background: var(--btn-bg); border: 1px solid var(--btn-border); color: var(--text-secondary); width: 48px; height: 48px; border-radius: 50%; cursor: pointer; display: flex; align-items: center; justify-content: center; font-size: 24px; transition: all 0.3s ease; z-index: 1000; box-shadow: 0 2px 8px var(--shadow); }
        .theme-toggle:hover { background: var(--btn-hover); transform: scale(1.1); border-color: var(--accent); }
        .tools-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 24px; margin-bottom: 40px; }
        .tool-category { background: var(--card-bg); border: 1px solid var(--card-border); border-radius: 12px; padding: 24px; backdrop-filter: blur(10px); transition: all 0.3s ease; }
        .tool-category h2 { color: var(--text-secondary); font-size: 18px; margin-bottom: 16px; padding-bottom: 8px; border-bottom: 2px solid var(--accent); }
        .tool-buttons { display: flex; flex-direction: column; gap: 10px; }
        .tool-btn { background: var(--btn-bg); border: 1px solid var(--btn-border); color: var(--text-secondary); padding: 12px 16px; border-radius: 8px; cursor: pointer; font-size: 14px; transition: all 0.3s ease; text-align: left; }
        .tool-btn:hover { background: var(--btn-hover); transform: translateX(4px); border-color: var(--accent); }
        .workspace { background: var(--workspace-bg); border: 1px solid var(--border-color); border-radius: 12px; padding: 32px; min-height: 400px; display: none; transition: all 0.3s ease; }
        .workspace.active { display: block; }
        .workspace h3 { color: var(--text-secondary); font-size: 24px; margin-bottom: 24px; }
        .upload-area { border: 2px dashed var(--border-hover); border-radius: 8px; padding: 40px; text-align: center; margin-bottom: 24px; cursor: pointer; transition: all 0.3s ease; }
        .upload-area:hover { border-color: var(--accent); background: var(--upload-bg); }
        .upload-area input[type="file"] { display: none; }
        .file-list { margin: 20px 0; padding: 16px; background: var(--card-bg); border-radius: 8px; }
        .file-item { padding: 8px 12px; margin: 4px 0; background: var(--file-bg); border-radius: 4px; display: flex; justify-content: space-between; align-items: center; }
        .action-btn { background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%); color: var(--text-primary); border: none; padding: 14px 32px; border-radius: 8px; font-size: 16px; font-weight: bold; cursor: pointer; transition: all 0.3s ease; margin-top: 16px; }
        .action-btn:hover { transform: translateY(-2px); box-shadow: 0 4px 12px rgba(47, 191, 135, 0.4); }
        .action-btn:disabled { opacity: 0.5; cursor: not-allowed; transform: none; }
        .back-btn { background: transparent; border: 1px solid var(--text-muted); color: var(--text-secondary); padding: 8px 16px; border-radius: 6px; cursor: pointer; margin-bottom: 20px; transition: all 0.3s ease; }
        .back-btn:hover { background: var(--btn-hover); border-color: var(--accent); }
        .footer { text-align: center; padding: 24px; color: var(--text-footer); font-size: 13px; border-top: 1px solid var(--border-color); margin-top: 40px; }
        .badge { display: inline-block; padding: 4px 12px; background: var(--btn-bg); border: 1px solid var(--btn-border); border-radius: 12px; font-size: 12px; color: var(--text-muted); margin: 4px; }
        .status { padding: 12px; border-radius: 6px; margin: 12px 0; display: none; }
        .status.show { display: block; }
        .status.success { background: var(--status-success-bg); border: 1px solid var(--accent); color: var(--text-secondary); }
        .status.error { background: var(--status-error-bg); border: 1px solid #e2574c; color: #f5b7b1; }
        .status.processing { background: var(--status-processing-bg); border: 1px solid var(--text-secondary); color: var(--text-secondary); }
        .status.warning { background: var(--status-warning-bg); border: 1px solid #f39c12; color: #f39c12; }
        .ocr-textarea { width: 100%; min-height: 300px; padding: 16px; background: var(--card-bg); border: 1px solid var(--border-color); border-radius: 8px; color: var(--text-primary); font-family: 'Courier New', monospace; font-size: 14px; resize: vertical; margin-top: 16px; }
        .ocr-actions { margin-top: 12px; display: flex; gap: 12px; flex-wrap: wrap; }
        .thumbnail-item { position: relative; border: 3px solid var(--border-color); border-radius: 8px; overflow: hidden; cursor: pointer; transition: all 0.2s ease; background: var(--bg-secondary); }
        .thumbnail-item:hover { border-color: var(--accent); transform: translateY(-2px); }
        .thumbnail-item.selected { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(47, 191, 135, 0.3); }
        .thumbnail-item img { width: 100%; height: auto; display: block; }
        .thumbnail-item .page-number { position: absolute; top: 4px; left: 4px; background: rgba(0,0,0,0.7); color: white; padding: 2px 8px; border-radius: 4px; font-size: 12px; font-weight: bold; }
        .thumbnail-item .check-mark { position: absolute; top: 4px; right: 4px; background: var(--accent); color: white; width: 24px; height: 24px; border-radius: 50%; display: none; align-items: center; justify-content: center; font-size: 14px; font-weight: bold; }
        .thumbnail-item.selected .check-mark { display: flex; }
        .thumbnail-item-split { position: relative; border: 3px solid var(--border-color); border-radius: 8px; overflow: hidden; background: var(--bg-secondary); cursor: pointer; transition: all 0.2s ease; }
        .thumbnail-item-split:hover { transform: translateY(-2px); box-shadow: 0 4px 8px var(--shadow); }
        .thumbnail-item-split img { width: 100%; height: auto; display: block; }
        .thumbnail-item-split .page-number { position: absolute; top: 4px; left: 4px; background: rgba(0,0,0,0.7); color: white; padding: 2px 8px; border-radius: 4px; font-size: 12px; font-weight: bold; }
        .thumbnail-item-split .group-badge { position: absolute; bottom: 4px; right: 4px; padding: 4px 8px; border-radius: 4px; font-size: 11px; font-weight: bold; color: white; display: none; }
        .thumbnail-item-split.assigned .group-badge { display: block; }
        .group-color-0 { background-color: #2fbf87 !important; }
        .group-color-1 { background-color: #3498db !important; }
        .group-color-2 { background-color: #e74c3c !important; }
        .group-color-3 { background-color: #f39c12 !important; }
        .group-color-4 { background-color: #9b59b6 !important; }
        .group-color-5 { background-color: #1abc9c !important; }
        .group-color-6 { background-color: #e67e22 !important; }
        .group-color-7 { background-color: #34495e !important; }
        .thumbnail-item-rotate { position: relative; border: 3px solid var(--border-color); border-radius: 8px; overflow: hidden; background: var(--bg-secondary); transition: all 0.2s ease; }
        .thumbnail-item-rotate:hover { transform: translateY(-2px); box-shadow: 0 4px 8px var(--shadow); }
        .thumbnail-item-rotate img { width: 100%; height: auto; display: block; }
        .thumbnail-item-rotate .page-number { position: absolute; top: 4px; left: 4px; background: rgba(0,0,0,0.7); color: white; padding: 2px 8px; border-radius: 4px; font-size: 12px; font-weight: bold; }
        .thumbnail-item-rotate .rotation-badge { position: absolute; top: 4px; right: 4px; background: var(--accent); color: white; padding: 2px 8px; border-radius: 4px; font-size: 11px; font-weight: bold; display: none; }
        .thumbnail-item-rotate.rotated .rotation-badge { display: block; }
        .rotate-controls { display: flex; gap: 4px; margin-top: 8px; }
        .rotate-controls button { flex: 1; padding: 6px 4px; background: var(--btn-bg); border: 1px solid var(--btn-border); color: var(--text-secondary); border-radius: 4px; cursor: pointer; font-size: 11px; transition: all 0.2s; }
        .rotate-controls button:hover { background: var(--btn-hover); border-color: var(--accent); }
        .rotate-controls button.active { background: var(--accent); color: var(--text-primary); border-color: var(--accent); }
    </style>
</head>
<body>
    <button class="theme-toggle" onclick="toggleTheme()" title="Cambiar modo claro/oscuro"><span id="theme-icon"></span></button>
    <div class="container">
        <div class="header">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 300" width="1000" height="300">
                <defs>
                    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#0f1a17"/><stop offset="100%" stop-color="#152522"/></linearGradient>
                    <linearGradient id="shieldG" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#2fbf87"/><stop offset="100%" stop-color="#14856a"/></linearGradient>
                </defs>
                <rect width="1000" height="300" fill="url(#bg)"/>
                <g opacity="0.07" stroke="#5fe0b0" stroke-width="1"><path d="M0 60 H1000 M0 120 H1000 M0 180 H1000 M0 240 H1000"/><path d="M700 0 V300 M760 0 V300 M820 0 V300 M880 0 V300 M940 0 V300"/></g>
                <g transform="translate(792 52)" opacity="0.5"><path d="M22 40 a15 15 0 0 1 3 -29.7 a20 20 0 0 1 38 6 a13 13 0 0 1 -2 23.7 Z" fill="none" stroke="#6b8a80" stroke-width="3" stroke-linejoin="round"/><line x1="8" y1="52" x2="76" y2="-6" stroke="#e2574c" stroke-width="4.5" stroke-linecap="round"/></g>
                <text x="795" y="140" font-family="'Courier New', monospace" font-size="13" fill="#6b8a80" letter-spacing="1">SIN NUBE</text>
                <g transform="translate(72 58)">
                    <path d="M84 6 L156 30 V96 C156 140 122 166 84 178 C46 166 12 140 12 96 V30 Z" fill="url(#shieldG)"/>
                    <path d="M84 18 L145 38 V96 C145 132 116 154 84 165 C52 154 23 132 23 96 V38 Z" fill="#0f1a17" opacity="0.35"/>
                    <g transform="translate(56 52)"><path d="M0 0 H36 L54 18 V80 H0 Z" fill="#f4f7f5"/><path d="M36 0 L54 18 H36 Z" fill="#c3d4cc"/><rect x="10" y="30" width="34" height="3.6" rx="1.8" fill="#8fa79d"/><rect x="10" y="41" width="34" height="3.6" rx="1.8" fill="#8fa79d"/><rect x="10" y="52" width="22" height="3.6" rx="1.8" fill="#8fa79d"/><text x="10" y="72" font-family="Helvetica, Arial, sans-serif" font-size="13" font-weight="bold" fill="#c0392b">PDF</text></g>
                </g>
                <text x="272" y="112" font-family="Helvetica, Arial, sans-serif" font-size="62" font-weight="300" fill="#8fd9bd" letter-spacing="-1">Local</text>
                <text x="418" y="112" font-family="Helvetica, Arial, sans-serif" font-size="62" font-weight="bold" fill="#f2f7f4" letter-spacing="-1">PDF Tools</text>
                <rect x="274" y="132" width="70" height="3" rx="1.5" fill="#2fbf87"/>
                <text x="274" y="176" font-family="Georgia, 'Times New Roman', serif" font-size="24" fill="#9dbdb0" font-style="italic">Tus documentos nunca salen de tu maquina.</text>
                <text x="274" y="218" font-family="'Courier New', monospace" font-size="15" fill="#6b8a80" letter-spacing="1.3">OCR · comprimir · convertir · unir · dividir · proteger</text>
                <text x="274" y="258" font-family="Helvetica, Arial, sans-serif" font-size="14" fill="#4c6459">Desarrollado por Matias Ardizzone</text>
            </svg>
        </div>
        <div class="tools-grid">
            <div class="tool-category"><h2>Combinar y separar</h2><div class="tool-buttons"><button class="tool-btn" onclick="showWorkspace('unir')">Unir PDFs</button><button class="tool-btn" onclick="showWorkspace('extraer')">Extraer paginas</button><button class="tool-btn" onclick="showWorkspace('dividir')">Dividir PDF</button></div></div>
            <div class="tool-category"><h2>Modificar</h2><div class="tool-buttons"><button class="tool-btn" onclick="showWorkspace('comprimir')">Comprimir PDF</button><button class="tool-btn" onclick="showWorkspace('rotar')">Rotar paginas</button></div></div>
            <div class="tool-category"><h2>Crear y revisar</h2><div class="tool-buttons"><button class="tool-btn" onclick="showWorkspace('pdf-a-word')">PDF a Word</button><button class="tool-btn" onclick="showWorkspace('office-to-pdf')">Office a PDF</button><button class="tool-btn" onclick="showWorkspace('ocr')">OCR (Extraer texto)</button></div></div>
        </div>
        <div id="workspace-unir" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>Unir PDFs</h3><div class="upload-area" onclick="document.getElementById('file-input-unir').click()"><input type="file" id="file-input-unir" multiple accept=".pdf" onchange="handleFilesUnir(this.files)"><p>Selecciona archivos PDF</p></div><div id="file-list-unir" class="file-list" style="display: none;"></div><div id="status-unir" class="status"></div><div style="display: flex; gap: 12px; margin-top: 16px;"><button id="btn-unir" class="action-btn" onclick="unirPDFs()" disabled style="display: none; margin-top: 0;">Unir PDFs</button><button id="btn-limpiar" class="action-btn" onclick="limpiarUnir()" style="display: none; margin-top: 0; background: var(--btn-bg); border: 1px solid var(--btn-border);">Limpiar</button></div></div>
        <div id="workspace-comprimir" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>Comprimir PDF</h3><div class="upload-area" onclick="document.getElementById('file-input-compress').click()"><input type="file" id="file-input-compress" accept=".pdf" onchange="handleFileCompress(this.files)"><p>Selecciona un PDF</p></div><div id="compress-options" style="display:none; margin: 20px 0; padding: 16px; background: var(--card-bg); border-radius: 8px;"><label style="color: var(--text-secondary); display: block; margin-bottom: 8px;">Nivel de compresión:</label><select id="compress-level" style="width: 100%; padding: 8px; background: var(--bg-secondary); color: var(--text-primary); border: 1px solid var(--border-color); border-radius: 4px;"><option value="baja">Baja (Mantiene mayor calidad)</option><option value="media" selected>Media (Recomendado)</option><option value="alta">Alta (Menor tamaño, puede perder calidad)</option></select></div><div id="status-compress" class="status"></div><button id="btn-compress" class="action-btn" onclick="comprimirPDF()" disabled style="display: none;">Comprimir</button></div>
        <div id="workspace-pdf-a-word" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>PDF a Word</h3><div class="upload-area" onclick="document.getElementById('file-input-word').click()"><input type="file" id="file-input-word" accept=".pdf" onchange="handleFileWord(this.files)"><p>Selecciona un PDF</p></div><div id="status-word" class="status"></div><button id="btn-word" class="action-btn" onclick="convertirAWord()" disabled style="display: none;">Convertir</button></div>
        <div id="workspace-office-to-pdf" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>Office a PDF</h3><div class="upload-area" onclick="document.getElementById('file-input-office').click()"><input type="file" id="file-input-office" accept=".doc,.docx,.xls,.xlsx" onchange="handleFileOffice(this.files)"><p>Selecciona un archivo de Office</p></div><div id="status-office" class="status"></div><button id="btn-office" class="action-btn" onclick="convertirOfficeAPdf()" disabled style="display: none;">Convertir</button></div>
        <div id="workspace-ocr" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>OCR - Reconocimiento de texto</h3><p style="margin-bottom: 20px; color: var(--text-secondary);">Aplica OCR a PDFs escaneados para hacerlos buscables o extraer el texto.</p><div class="upload-area" onclick="document.getElementById('file-input-ocr').click()"><input type="file" id="file-input-ocr" accept=".pdf" onchange="handleFileOCR(this.files)"><p>Selecciona un PDF escaneado</p></div><div id="status-ocr" class="status"></div><div id="ocr-buttons" style="display: none; margin-top: 20px; display: flex; gap: 12px; flex-wrap: wrap;"><button id="btn-ocr-txt" class="action-btn" onclick="extraerTextoOCR()" style="margin-top: 0;">Extraer a TXT</button><button id="btn-ocr-pdf" class="action-btn" onclick="aplicarOCRaPDF()" style="margin-top: 0; background: linear-gradient(135deg, #14856a 0%, #0f1a17 100%);">Aplicar OCR al PDF</button></div><div id="ocr-result" style="display: none;"><h4 style="color: var(--text-secondary); margin-top: 24px; margin-bottom: 8px;">Texto extraido:</h4><textarea id="ocr-text" class="ocr-textarea" readonly></textarea><div class="ocr-actions"><button class="action-btn" onclick="copiarTextoOCR()" style="margin-top: 0;">Copiar al portapapeles</button><button class="action-btn" onclick="guardarTextoOCR()" style="margin-top: 0; background: var(--btn-bg); border: 1px solid var(--btn-border);">Guardar como .txt</button></div></div></div>
        <div id="workspace-extraer" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>Extraer páginas de un PDF</h3><p style="margin-bottom: 20px; color: var(--text-secondary);">Selecciona un PDF y hacé clic en las páginas que querés extraer.</p><div class="upload-area" onclick="document.getElementById('file-input-extract').click()"><input type="file" id="file-input-extract" accept=".pdf" onchange="handleFileExtract(this.files)"><p>Selecciona un PDF</p></div><div id="thumbnails-container" style="display:none; margin: 20px 0;"><div style="display: flex; gap: 10px; margin-bottom: 16px; flex-wrap: wrap;"><button class="action-btn" onclick="selectAllPages()" style="margin-top: 0; padding: 8px 16px; font-size: 14px;">Seleccionar todas</button><button class="action-btn" onclick="deselectAllPages()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: var(--btn-bg); border: 1px solid var(--btn-border);">Deseleccionar todas</button><button class="action-btn" onclick="invertSelection()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: var(--btn-bg); border: 1px solid var(--btn-border);">Invertir selección</button><button class="action-btn" onclick="limpiarExtraccion()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: rgba(226, 87, 76, 0.2); border: 1px solid #e2574c; color: #f5b7b1;">Limpiar todo</button><span id="selected-count" style="color: var(--text-secondary); font-size: 14px; align-self: center; margin-left: auto;">0 páginas seleccionadas</span></div><div id="thumbnails-grid" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 16px; max-height: 500px; overflow-y: auto; padding: 16px; background: var(--card-bg); border-radius: 8px;"></div></div><div id="status-extract" class="status"></div><button id="btn-extract" class="action-btn" onclick="extraerPaginas()" disabled style="display: none;">Extraer páginas seleccionadas</button></div>
        <div id="workspace-dividir" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>Dividir PDF en partes</h3><p style="margin-bottom: 20px; color: var(--text-secondary);">Selecciona un PDF, creá grupos y asigná páginas a cada uno con un clic.</p><div class="upload-area" onclick="document.getElementById('file-input-split').click()"><input type="file" id="file-input-split" accept=".pdf" onchange="handleFileSplit(this.files)"><p>Selecciona un PDF</p></div><div id="split-controls" style="display:none; margin: 20px 0;"><div style="display: flex; gap: 12px; margin-bottom: 16px; flex-wrap: wrap; align-items: center; padding: 16px; background: var(--card-bg); border-radius: 8px;"><label style="color: var(--text-secondary); font-size: 14px;">Grupo activo:</label><select id="active-group-select" onchange="changeActiveGroup(this.value)" style="padding: 8px 12px; background: var(--bg-secondary); color: var(--text-primary); border: 1px solid var(--border-color); border-radius: 4px; font-size: 14px;"></select><button class="action-btn" onclick="createNewGroup()" style="margin-top: 0; padding: 8px 16px; font-size: 14px;">+ Nuevo grupo</button><button class="action-btn" onclick="splitAllPages()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: var(--btn-bg); border: 1px solid var(--btn-border);">Dividir todo (1 pág c/u)</button><button class="action-btn" onclick="limpiarDivision()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: rgba(226, 87, 76, 0.2); border: 1px solid #e2574c; color: #f5b7b1;">Limpiar</button><span id="split-summary" style="color: var(--text-muted); font-size: 13px; margin-left: auto;"></span></div><div id="thumbnails-grid-split" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); gap: 12px; max-height: 500px; overflow-y: auto; padding: 16px; background: var(--card-bg); border-radius: 8px;"></div></div><div id="status-split" class="status"></div><button id="btn-split" class="action-btn" onclick="generarDivision()" disabled style="display: none; margin-top: 20px;">Generar ZIP con PDFs divididos</button></div>
        <div id="workspace-rotar" class="workspace"><button class="back-btn" onclick="backToMenu()">Volver al menu</button><h3>Rotar páginas de un PDF</h3><p style="margin-bottom: 20px; color: var(--text-secondary);">Selecciona un PDF y rotá las páginas que necesites. Usá los botones para girar cada página.</p><div class="upload-area" onclick="document.getElementById('file-input-rotate').click()"><input type="file" id="file-input-rotate" accept=".pdf" onchange="handleFileRotate(this.files)"><p>Selecciona un PDF</p></div><div id="rotate-controls-bar" style="display:none; margin: 20px 0;"><div style="display: flex; gap: 10px; margin-bottom: 16px; flex-wrap: wrap; align-items: center; padding: 16px; background: var(--card-bg); border-radius: 8px;"><button class="action-btn" onclick="rotateAllPages(90)" style="margin-top: 0; padding: 8px 16px; font-size: 14px;">Rotar todas 90°</button><button class="action-btn" onclick="rotateAllPages(180)" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: var(--btn-bg); border: 1px solid var(--btn-border);">Rotar todas 180°</button><button class="action-btn" onclick="rotateAllPages(270)" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: var(--btn-bg); border: 1px solid var(--btn-border);">Rotar todas 270°</button><button class="action-btn" onclick="resetAllRotations()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: var(--btn-bg); border: 1px solid var(--btn-border);">Resetear todas</button><button class="action-btn" onclick="limpiarRotacion()" style="margin-top: 0; padding: 8px 16px; font-size: 14px; background: rgba(226, 87, 76, 0.2); border: 1px solid #e2574c; color: #f5b7b1;">Limpiar</button><span id="rotate-summary" style="color: var(--text-muted); font-size: 13px; margin-left: auto;">0 páginas rotadas</span></div><div id="thumbnails-grid-rotate" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 12px; max-height: 500px; overflow-y: auto; padding: 16px; background: var(--card-bg); border-radius: 8px;"></div></div><div id="status-rotate" class="status"></div><button id="btn-rotate" class="action-btn" onclick="generarRotacion()" disabled style="display: none; margin-top: 20px;">Guardar PDF rotado</button></div>
        <div class="footer"><span class="badge">100% local</span><span class="badge">Sin internet</span><p style="margin-top: 12px;">Local PDF Tools v0.2.0 · Matias Ardizzone</p></div>
    </div>
    <script>
        function initTheme() { const t = localStorage.getItem('theme') || 'dark'; document.documentElement.setAttribute('data-theme', t); document.getElementById('theme-icon').textContent = t === 'light' ? '☀️' : ''; }
        function toggleTheme() { const c = document.documentElement.getAttribute('data-theme'); const n = c === 'light' ? 'dark' : 'light'; document.documentElement.setAttribute('data-theme', n); localStorage.setItem('theme', n); document.getElementById('theme-icon').textContent = n === 'light' ? '☀️' : '🌙'; }
        initTheme();
        let pdfFilesUnir = [], fileWord = null, fileCompress = null, fileOffice = null, fileOCR = null, fileExtract = null, fileSplit = null, fileRotate = null;
        function showWorkspace(t) { document.querySelectorAll('.workspace').forEach(w => w.classList.remove('active')); const w = document.getElementById('workspace-' + t); if(w) { w.classList.add('active'); w.scrollIntoView({behavior:'smooth'}); } }
        function backToMenu() { if (document.getElementById('workspace-extraer').classList.contains('active')) { limpiarExtraccion(); } if (document.getElementById('workspace-dividir').classList.contains('active')) { limpiarDivision(); } if (document.getElementById('workspace-rotar').classList.contains('active')) { limpiarRotacion(); } document.querySelectorAll('.workspace').forEach(w => w.classList.remove('active')); window.scrollTo({top:0, behavior:'smooth'}); }
        function showStatus(id, msg, type) { const s = document.getElementById(id); s.innerHTML = msg; s.className = 'status show ' + type; }
        function handleFilesUnir(files) { pdfFilesUnir = Array.from(files).filter(f => f.type === 'application/pdf' || f.name.endsWith('.pdf')); const fl = document.getElementById('file-list-unir'), btn = document.getElementById('btn-unir'); if(pdfFilesUnir.length === 0) { fl.style.display = 'none'; btn.style.display = 'none'; btn.disabled = true; showStatus('status-unir', 'No se seleccionaron PDFs', 'error'); return; } fl.innerHTML = '<strong>Archivos:</strong>'; pdfFilesUnir.forEach((f, i) => { const d = document.createElement('div'); d.className = 'file-item'; d.innerHTML = '<span>' + (i+1) + '. ' + f.name + '</span><button onclick="removeFileUnir('+i+')" style="background:none;border:none;color:#e2574c;cursor:pointer;">X</button>'; fl.appendChild(d); }); fl.style.display = 'block'; btn.style.display = 'inline-block'; btn.disabled = pdfFilesUnir.length < 2; showStatus('status-unir', pdfFilesUnir.length < 2 ? 'Selecciona al menos 2 PDFs' : 'Listo para unir', pdfFilesUnir.length < 2 ? 'processing' : 'success'); }
        function removeFileUnir(i) { pdfFilesUnir.splice(i, 1); handleFilesUnir(pdfFilesUnir); }
        async function unirPDFs() { if(pdfFilesUnir.length < 2) return; showStatus('status-unir', 'Procesando...', 'processing'); const btn = document.getElementById('btn-unir'); btn.disabled = true; btn.textContent = 'Uniendo...'; const fd = new FormData(); pdfFilesUnir.forEach(f => fd.append('files', f)); try { const res = await fetch(window.location.origin + '/api/merge', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog('unido.pdf'); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-unir', 'PDF unido guardado en: <br><strong>' + savePath + '</strong>', 'success'); document.getElementById('btn-limpiar').style.display = 'inline-block'; } else { showStatus('status-unir', 'Operacion cancelada', 'warning'); } } catch(e) { showStatus('status-unir', 'Error: ' + e.message, 'error'); } finally { btn.disabled = false; btn.textContent = 'Unir PDFs'; } }
        function limpiarUnir() { pdfFilesUnir = []; document.getElementById('file-list-unir').innerHTML = ''; document.getElementById('file-list-unir').style.display = 'none'; document.getElementById('btn-unir').style.display = 'none'; document.getElementById('btn-unir').disabled = true; document.getElementById('btn-limpiar').style.display = 'none'; document.getElementById('file-input-unir').value = ''; const s = document.getElementById('status-unir'); s.innerHTML = ''; s.className = 'status'; }
        function handleFileCompress(files) { if(!files.length) { document.getElementById('compress-options').style.display = 'none'; return; } fileCompress = files[0]; document.getElementById('status-compress').textContent = 'Seleccionado: ' + fileCompress.name; document.getElementById('status-compress').className = 'status show success'; document.getElementById('compress-options').style.display = 'block'; document.getElementById('btn-compress').style.display = 'inline-block'; document.getElementById('btn-compress').disabled = false; }
        async function comprimirPDF() { if(!fileCompress) return; showStatus('status-compress', 'Comprimiendo...', 'processing'); const btn = document.getElementById('btn-compress'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileCompress); fd.append('level', document.getElementById('compress-level').value); try { const res = await fetch(window.location.origin + '/api/compress', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileCompress.name.replace('.pdf', '_comprimido.pdf')); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-compress', 'PDF comprimido guardado en: <br><strong>' + savePath + '</strong>', 'success'); } else { showStatus('status-compress', 'Operacion cancelada', 'warning'); } } catch(e) { showStatus('status-compress', 'Error: ' + e.message, 'error'); } finally { btn.disabled = false; btn.textContent = 'Comprimir'; } }
        function handleFileWord(files) { if(!files.length) return; fileWord = files[0]; document.getElementById('status-word').textContent = 'Seleccionado: ' + fileWord.name; document.getElementById('status-word').className = 'status show success'; document.getElementById('btn-word').style.display = 'inline-block'; document.getElementById('btn-word').disabled = false; }
        async function convertirAWord() { if(!fileWord) return; showStatus('status-word', 'Convirtiendo...', 'processing'); const btn = document.getElementById('btn-word'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileWord); try { const res = await fetch(window.location.origin + '/api/pdf-to-word', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileWord.name.replace('.pdf', '.docx')); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-word', 'Word guardado en: <br><strong>' + savePath + '</strong>', 'success'); } else { showStatus('status-word', 'Operacion cancelada', 'warning'); } } catch(e) { showStatus('status-word', 'Error: ' + e.message, 'error'); } finally { btn.disabled = false; btn.textContent = 'Convertir'; } }
        function handleFileOffice(files) { if(!files.length) return; fileOffice = files[0]; document.getElementById('status-office').textContent = 'Seleccionado: ' + fileOffice.name; document.getElementById('status-office').className = 'status show success'; document.getElementById('btn-office').style.display = 'inline-block'; document.getElementById('btn-office').disabled = false; }
        async function convertirOfficeAPdf() { if(!fileOffice) return; showStatus('status-office', 'Convirtiendo...', 'processing'); const btn = document.getElementById('btn-office'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileOffice); try { const res = await fetch(window.location.origin + '/api/office-to-pdf', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileOffice.name.replace(/\\.[^/.]+$/, '') + '.pdf'); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-office', 'PDF guardado en: <br><strong>' + savePath + '</strong>', 'success'); } else { showStatus('status-office', 'Operacion cancelada', 'warning'); } } catch(e) { showStatus('status-office', 'Error: ' + e.message, 'warning'); } finally { btn.disabled = false; btn.textContent = 'Convertir'; } }
        function handleFileOCR(files) { if(!files.length) { document.getElementById('ocr-buttons').style.display = 'none'; return; } fileOCR = files[0]; document.getElementById('status-ocr').textContent = 'Seleccionado: ' + fileOCR.name; document.getElementById('status-ocr').className = 'status show success'; document.getElementById('ocr-buttons').style.display = 'flex'; document.getElementById('ocr-result').style.display = 'none'; }
        async function extraerTextoOCR() { if(!fileOCR) return; showStatus('status-ocr', 'Procesando OCR (puede tardar)...', 'processing'); const btn = document.getElementById('btn-ocr-txt'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileOCR); try { const res = await fetch(window.location.origin + '/api/ocr', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const text = await res.text(); document.getElementById('ocr-text').value = text; document.getElementById('ocr-result').style.display = 'block'; showStatus('status-ocr', 'Texto extraido exitosamente!', 'success'); document.getElementById('ocr-result').scrollIntoView({behavior:'smooth'}); } catch(e) { showStatus('status-ocr', e.message, 'warning'); } finally { btn.disabled = false; btn.textContent = 'Extraer a TXT'; } }
        async function aplicarOCRaPDF() { if(!fileOCR) return; showStatus('status-ocr', 'Aplicando OCR... (puede tardar)', 'processing'); const btn = document.getElementById('btn-ocr-pdf'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileOCR); try { const res = await fetch(window.location.origin + '/api/ocr-to-pdf', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileOCR.name.replace('.pdf', '_ocr.pdf')); if(savePath) { await window.pywebview.api.saveFile(data.pdf_path, savePath); showStatus('status-ocr', 'PDF guardado exitosamente en: <br><strong>' + savePath + '</strong>', 'success'); } else { showStatus('status-ocr', 'Operacion cancelada', 'warning'); } } catch(e) { showStatus('status-ocr', e.message, 'warning'); } finally { btn.disabled = false; btn.textContent = 'Aplicar OCR al PDF'; } }
        function copiarTextoOCR() { const textarea = document.getElementById('ocr-text'); textarea.select(); textarea.setSelectionRange(0, 99999); navigator.clipboard.writeText(textarea.value).then(() => { showStatus('status-ocr', 'Texto copiado al portapapeles!', 'success'); }).catch(() => { document.execCommand('copy'); showStatus('status-ocr', 'Texto copiado!', 'success'); }); }
        function guardarTextoOCR() { const text = document.getElementById('ocr-text').value; if(!text) { showStatus('status-ocr', 'No hay texto para guardar', 'warning'); return; } fetch(window.location.origin + '/api/save-txt', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({text: text, name: fileOCR.name.replace('.pdf', '_ocr.txt')})}).then(r => r.json()).then(d2 => { showStatus('status-ocr', 'TXT guardado en: <strong>' + d2.saved + '</strong>', 'success'); }).catch(e => { showStatus('status-ocr', 'Error: ' + e.message, 'warning'); }); }
        let selectedPages = new Set();
        async function handleFileExtract(files) { if(!files.length) { document.getElementById('thumbnails-container').style.display = 'none'; document.getElementById('btn-extract').style.display = 'none'; return; } fileExtract = files[0]; document.getElementById('status-extract').textContent = 'Cargando miniaturas...'; document.getElementById('status-extract').className = 'status show processing'; document.getElementById('thumbnails-container').style.display = 'none'; document.getElementById('btn-extract').style.display = 'none'; selectedPages.clear(); const fd = new FormData(); fd.append('file', fileExtract); try { const res = await fetch(window.location.origin + '/api/get-thumbnails', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const grid = document.getElementById('thumbnails-grid'); grid.innerHTML = ''; data.thumbnails.forEach(t => { const div = document.createElement('div'); div.className = 'thumbnail-item'; div.dataset.page = t.page; div.onclick = () => togglePage(t.page, div); div.innerHTML = '<span class="page-number">' + t.page + '</span><span class="check-mark">✓</span><img src="' + t.image + '" alt="Página ' + t.page + '">'; grid.appendChild(div); }); document.getElementById('thumbnails-container').style.display = 'block'; document.getElementById('status-extract').className = 'status'; updateSelectedCount(); } catch(e) { showStatus('status-extract', 'Error al cargar miniaturas: ' + e.message, 'error'); } }
        function togglePage(page, element) { if(selectedPages.has(page)) { selectedPages.delete(page); element.classList.remove('selected'); } else { selectedPages.add(page); element.classList.add('selected'); } updateSelectedCount(); }
        function selectAllPages() { document.querySelectorAll('.thumbnail-item').forEach(el => { el.classList.add('selected'); selectedPages.add(parseInt(el.dataset.page)); }); updateSelectedCount(); }
        function deselectAllPages() { document.querySelectorAll('.thumbnail-item').forEach(el => { el.classList.remove('selected'); }); selectedPages.clear(); updateSelectedCount(); }
        function invertSelection() { document.querySelectorAll('.thumbnail-item').forEach(el => { const page = parseInt(el.dataset.page); if(selectedPages.has(page)) { selectedPages.delete(page); el.classList.remove('selected'); } else { selectedPages.add(page); el.classList.add('selected'); } }); updateSelectedCount(); }
        function updateSelectedCount() { const count = selectedPages.size; document.getElementById('selected-count').textContent = count + ' página' + (count !== 1 ? 's' : '') + ' seleccionada' + (count !== 1 ? 's' : ''); const btn = document.getElementById('btn-extract'); btn.style.display = count > 0 ? 'inline-block' : 'none'; btn.disabled = count === 0; }
        async function extraerPaginas() { if(!fileExtract || selectedPages.size === 0) return; const pages = Array.from(selectedPages).sort((a,b) => a-b).join(','); showStatus('status-extract', 'Extrayendo páginas...', 'processing'); const btn = document.getElementById('btn-extract'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileExtract); fd.append('pages', pages); try { const res = await fetch(window.location.origin + '/api/extract-pages', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileExtract.name.replace('.pdf', '_extraido.pdf')); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-extract', 'PDF extraído guardado en: <br><strong>' + savePath + '</strong>', 'success'); } else { showStatus('status-extract', 'Operación cancelada', 'warning'); } } catch(e) { showStatus('status-extract', 'Error: ' + e.message, 'error'); } finally { btn.disabled = false; btn.textContent = 'Extraer páginas seleccionadas'; } }
        function limpiarExtraccion() { selectedPages.clear(); fileExtract = null; document.getElementById('thumbnails-grid').innerHTML = ''; document.getElementById('thumbnails-container').style.display = 'none'; document.getElementById('btn-extract').style.display = 'none'; document.getElementById('file-input-extract').value = ''; const s = document.getElementById('status-extract'); if(s) { s.innerHTML = ''; s.className = 'status'; } }
        let splitGroups = [];
        let splitPageAssignments = {};
        let activeGroupIndex = 0;
        const groupColors = ['#2fbf87', '#3498db', '#e74c3c', '#f39c12', '#9b59b6', '#1abc9c', '#e67e22', '#34495e'];
        async function handleFileSplit(files) { if(!files.length) { document.getElementById('split-controls').style.display = 'none'; document.getElementById('btn-split').style.display = 'none'; return; } fileSplit = files[0]; document.getElementById('status-split').textContent = 'Cargando miniaturas...'; document.getElementById('status-split').className = 'status show processing'; document.getElementById('split-controls').style.display = 'none'; document.getElementById('btn-split').style.display = 'none'; splitGroups = ['Grupo 1']; splitPageAssignments = {}; activeGroupIndex = 0; const fd = new FormData(); fd.append('file', fileSplit); try { const res = await fetch(window.location.origin + '/api/get-thumbnails', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const grid = document.getElementById('thumbnails-grid-split'); grid.innerHTML = ''; for(let i = 0; i < data.total; i++) { const pageNum = i + 1; const thumb = data.thumbnails[i]; const div = document.createElement('div'); div.className = 'thumbnail-item-split'; div.dataset.page = pageNum; div.onclick = () => togglePageSplit(pageNum, div); div.innerHTML = '<span class="page-number">Pág ' + pageNum + '</span><span class="group-badge"></span><img src="' + thumb.image + '" alt="Página ' + pageNum + '">'; grid.appendChild(div); } updateGroupSelect(); document.getElementById('split-controls').style.display = 'block'; document.getElementById('btn-split').style.display = 'inline-block'; document.getElementById('btn-split').disabled = false; document.getElementById('status-split').className = 'status'; updateSplitSummary(); } catch(e) { showStatus('status-split', 'Error al cargar miniaturas: ' + e.message, 'error'); } }
        function updateGroupSelect() { const select = document.getElementById('active-group-select'); select.innerHTML = ''; splitGroups.forEach((g, idx) => { const opt = document.createElement('option'); opt.value = idx; opt.textContent = g; if(idx === activeGroupIndex) opt.selected = true; select.appendChild(opt); }); }
        function changeActiveGroup(idx) { activeGroupIndex = parseInt(idx); }
        function createNewGroup() { const name = prompt('Nombre del nuevo grupo:', 'Grupo ' + (splitGroups.length + 1)); if(name && name.trim()) { splitGroups.push(name.trim()); updateGroupSelect(); document.getElementById('active-group-select').value = splitGroups.length - 1; activeGroupIndex = splitGroups.length - 1; updateSplitSummary(); } }
        function togglePageSplit(pageNum, element) { if(splitPageAssignments[pageNum] !== undefined) { delete splitPageAssignments[pageNum]; element.classList.remove('assigned'); element.style.borderColor = 'var(--border-color)'; element.querySelector('.group-badge').textContent = ''; } else { splitPageAssignments[pageNum] = activeGroupIndex; element.classList.add('assigned'); element.style.borderColor = groupColors[activeGroupIndex % groupColors.length]; const badge = element.querySelector('.group-badge'); badge.textContent = splitGroups[activeGroupIndex]; badge.className = 'group-badge group-color-' + (activeGroupIndex % groupColors.length); } updateSplitSummary(); }
        function splitAllPages() { const total = document.querySelectorAll('.thumbnail-item-split').length; splitGroups = []; splitPageAssignments = {}; for(let i = 0; i < total; i++) { const pageNum = i + 1; const groupName = 'Parte ' + pageNum; splitGroups.push(groupName); splitPageAssignments[pageNum] = i; const el = document.querySelectorAll('.thumbnail-item-split')[i]; el.classList.add('assigned'); el.style.borderColor = groupColors[i % groupColors.length]; const badge = el.querySelector('.group-badge'); badge.textContent = groupName; badge.className = 'group-badge group-color-' + (i % groupColors.length); } activeGroupIndex = 0; updateGroupSelect(); updateSplitSummary(); }
        function updateSplitSummary() { const assigned = Object.keys(splitPageAssignments).length; const total = document.querySelectorAll('.thumbnail-item-split').length; const summary = splitGroups.length + ' grupo(s) · ' + assigned + '/' + total + ' páginas asignadas'; document.getElementById('split-summary').textContent = summary; const btn = document.getElementById('btn-split'); btn.disabled = assigned === 0; }
        function limpiarDivision() { fileSplit = null; splitGroups = ['Grupo 1']; splitPageAssignments = {}; activeGroupIndex = 0; document.getElementById('thumbnails-grid-split').innerHTML = ''; document.getElementById('split-controls').style.display = 'none'; document.getElementById('btn-split').style.display = 'none'; document.getElementById('btn-split').disabled = true; document.getElementById('file-input-split').value = ''; const s = document.getElementById('status-split'); if(s) { s.innerHTML = ''; s.className = 'status'; } }
        async function generarDivision() { if(!fileSplit || Object.keys(splitPageAssignments).length === 0) { showStatus('status-split', 'No hay páginas asignadas', 'warning'); return; } const groups = {}; splitGroups.forEach((g, idx) => groups[g] = []); Object.entries(splitPageAssignments).forEach(([page, groupIdx]) => { const groupName = splitGroups[groupIdx]; if(groups[groupName] !== undefined) { groups[groupName].push(parseInt(page) - 1); } }); const nonEmptyGroups = {}; Object.entries(groups).forEach(([k, v]) => { if(v.length > 0) nonEmptyGroups[k] = v; }); showStatus('status-split', 'Generando ZIP...', 'processing'); const btn = document.getElementById('btn-split'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileSplit); fd.append('groups', JSON.stringify(nonEmptyGroups)); try { const res = await fetch(window.location.origin + '/api/split-pdf', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileSplit.name.replace('.pdf', '_dividido.zip')); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-split', 'ZIP guardado en: <br><strong>' + savePath + '</strong><br><br>Descomprimilo para ver los PDFs individuales.', 'success'); } else { showStatus('status-split', 'Operación cancelada', 'warning'); } } catch(e) { showStatus('status-split', 'Error: ' + e.message, 'error'); } finally { btn.disabled = false; btn.textContent = 'Generar ZIP con PDFs divididos'; } }
        let fileRotate = null;
        let pageRotations = {};
        async function handleFileRotate(files) { if(!files.length) { document.getElementById('rotate-controls-bar').style.display = 'none'; document.getElementById('btn-rotate').style.display = 'none'; return; } fileRotate = files[0]; document.getElementById('status-rotate').textContent = 'Cargando miniaturas...'; document.getElementById('status-rotate').className = 'status show processing'; document.getElementById('rotate-controls-bar').style.display = 'none'; document.getElementById('btn-rotate').style.display = 'none'; pageRotations = {}; const fd = new FormData(); fd.append('file', fileRotate); try { const res = await fetch(window.location.origin + '/api/get-thumbnails', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const grid = document.getElementById('thumbnails-grid-rotate'); grid.innerHTML = ''; for(let i = 0; i < data.total; i++) { const pageNum = i + 1; const thumb = data.thumbnails[i]; pageRotations[pageNum] = 0; const div = document.createElement('div'); div.className = 'thumbnail-item-rotate'; div.dataset.page = pageNum; div.innerHTML = '<span class="page-number">Pág ' + pageNum + '</span><span class="rotation-badge">0°</span><img src="' + thumb.image + '" alt="Página ' + pageNum + '"><div class="rotate-controls"><button onclick="setRotation(' + pageNum + ', 0, this)">0°</button><button onclick="setRotation(' + pageNum + ', 90, this)">90°</button><button onclick="setRotation(' + pageNum + ', 180, this)">180°</button><button onclick="setRotation(' + pageNum + ', 270, this)">270°</button></div>'; grid.appendChild(div); const btns = div.querySelectorAll('.rotate-controls button'); btns[0].classList.add('active'); } document.getElementById('rotate-controls-bar').style.display = 'block'; document.getElementById('btn-rotate').style.display = 'inline-block'; document.getElementById('btn-rotate').disabled = false; document.getElementById('status-rotate').className = 'status'; updateRotateSummary(); } catch(e) { showStatus('status-rotate', 'Error al cargar miniaturas: ' + e.message, 'error'); } }
        function setRotation(pageNum, angle, btn) { pageRotations[pageNum] = angle; const div = btn.closest('.thumbnail-item-rotate'); const badge = div.querySelector('.rotation-badge'); badge.textContent = angle + '°'; if(angle !== 0) { div.classList.add('rotated'); } else { div.classList.remove('rotated'); } div.querySelectorAll('.rotate-controls button').forEach(b => b.classList.remove('active')); btn.classList.add('active'); updateRotateSummary(); }
        function rotateAllPages(angle) { document.querySelectorAll('.thumbnail-item-rotate').forEach(div => { const pageNum = parseInt(div.dataset.page); pageRotations[pageNum] = angle; const badge = div.querySelector('.rotation-badge'); badge.textContent = angle + '°'; if(angle !== 0) { div.classList.add('rotated'); } else { div.classList.remove('rotated'); } div.querySelectorAll('.rotate-controls button').forEach(b => { b.classList.remove('active'); if(parseInt(b.textContent) === angle) b.classList.add('active'); }); }); updateRotateSummary(); }
        function resetAllRotations() { rotateAllPages(0); }
        function updateRotateSummary() { const rotated = Object.values(pageRotations).filter(a => a !== 0).length; const total = Object.keys(pageRotations).length; document.getElementById('rotate-summary').textContent = rotated + '/' + total + ' páginas rotadas'; const btn = document.getElementById('btn-rotate'); btn.disabled = rotated === 0; }
        function limpiarRotacion() { fileRotate = null; pageRotations = {}; document.getElementById('thumbnails-grid-rotate').innerHTML = ''; document.getElementById('rotate-controls-bar').style.display = 'none'; document.getElementById('btn-rotate').style.display = 'none'; document.getElementById('btn-rotate').disabled = true; document.getElementById('file-input-rotate').value = ''; const s = document.getElementById('status-rotate'); if(s) { s.innerHTML = ''; s.className = 'status'; } }
        async function generarRotacion() { if(!fileRotate) return; const rotations = {}; Object.entries(pageRotations).forEach(([page, angle]) => { if(angle !== 0) rotations[page - 1] = angle; }); if(Object.keys(rotations).length === 0) { showStatus('status-rotate', 'No hay páginas para rotar', 'warning'); return; } showStatus('status-rotate', 'Aplicando rotaciones...', 'processing'); const btn = document.getElementById('btn-rotate'); btn.disabled = true; btn.textContent = 'Procesando...'; const fd = new FormData(); fd.append('file', fileRotate); fd.append('rotations', JSON.stringify(rotations)); try { const res = await fetch(window.location.origin + '/api/rotate-pdf', {method:'POST', body:fd}); if(!res.ok) { const err = await res.json(); throw new Error(err.message || err.error); } const data = await res.json(); const savePath = await window.pywebview.api.saveFileDialog(fileRotate.name.replace('.pdf', '_rotado.pdf')); if(savePath) { await window.pywebview.api.saveFile(data.file_path, savePath); showStatus('status-rotate', 'PDF rotado guardado en: <br><strong>' + savePath + '</strong>', 'success'); } else { showStatus('status-rotate', 'Operación cancelada', 'warning'); } } catch(e) { showStatus('status-rotate', 'Error: ' + e.message, 'error'); } finally { btn.disabled = false; btn.textContent = 'Guardar PDF rotado'; } }
        window.addEventListener('beforeunload', () => { navigator.sendBeacon('/api/shutdown'); });
    </script>
</body>
</html>
'''

with open(html_path, "w", encoding="utf-8") as f:
    f.write(html_content)

print("OK: HTML restaurado con el diseño profesional de v0.1.0")
print("\nEjecutá: python app\\launcher.py")